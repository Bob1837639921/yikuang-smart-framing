<view class="mini-header {{dark ? 'dark-header' : ''}} {{inset ? 'inset-header' : ''}}">
  <button class="back-button" bindtap="handleBack" aria-label="返回">
    <view class="back-arrow"></view>
  </button>
  <text class="header-title">{{title}}</text>
  <view wx:if="{{step}}" class="header-step"><text class="header-step-label">步骤</text><text class="header-step-value">{{step}}</text></view>
</view>
